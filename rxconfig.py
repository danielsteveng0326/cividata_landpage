import reflex as rx

config = rx.Config(
    app_name="cividata_company_landing_page",
    frontend_port=3000,  # Cambia esto al puerto que quieras
    backend_port=8000,   # Cambia esto también si es necesario
)
